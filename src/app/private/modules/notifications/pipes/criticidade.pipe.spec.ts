import { CriticidadePipe } from './criticidade.pipe';

describe('CriticidadePipe', () => {
  it('create an instance', () => {
    const pipe = new CriticidadePipe();
    expect(pipe).toBeTruthy();
  });
});
